<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CityWalk" parent="Theme.MaterialComponents.DayNight.DarkActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- 设置启动页的 style -->
    <style name="Theme.Pager" parent="Theme.CityWalk" >
        <item name="android:windowBackground">@drawable/loading</item> // 设置启动页背景图片
        <item name="android:windowFullscreen">true</item> // 设置背景图片为全屏显示
        <item name="android:navigationBarColor">@android:color/transparent</item> // 设置导航栏透明

    </style>

</resources>